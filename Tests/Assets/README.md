# Test Assets

Stage 2.6.a manual-QA video asset.

Place a short recording of a Taegeuk 1 performance here as:

    Tests/Assets/taegeuk_1_sample.mov

It is used only for manual QA of the pose-extraction and analysis UI on a
simulator / device — the automated unit tests use synthetic pose data and do
not need it. The file is intentionally not committed; supply it locally.
