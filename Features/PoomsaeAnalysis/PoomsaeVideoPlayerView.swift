#if os(iOS)
import SwiftUI
import AVFoundation

// MARK: - PoomsaeVideoPlayerView
//
// Stage 2.6.a — a `UIViewRepresentable` wrapping an `AVPlayerLayer`-backed
// `UIView` for the imported poomsae video.
//
// Exposes the current playback time (via a periodic time observer) so the
// skeleton overlay and the motion-energy playhead stay in sync, and accepts
// seek requests from the tap-to-seek interactions (Task 6).

// MARK: - PoomsaeVideoPlayerView (UIViewRepresentable)

// MARK: - PlayerContainerView (AVPlayerLayer host)
#endif
