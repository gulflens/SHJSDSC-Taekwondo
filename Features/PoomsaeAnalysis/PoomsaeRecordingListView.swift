#if os(iOS)
import SwiftUI
import PhotosUI

// MARK: - PoomsaeRecordingListView
//
// Stage 2.6.a — screen (a): the imported-recordings list.
//
// Rows show a thumbnail, original filename, formatted duration and relative
// import date, sorted by `importedAt` descending. A `PhotosPicker` toolbar
// button (videos only) drives import. Friendly empty state when there are no
// recordings. Tapping a row pushes `PoomsaeAnalysisView`.
//
// iPad-only feature (see Stage 2.6.a design — macOS deferred).

// MARK: - PoomsaeRecordingListView

// MARK: - RecordingRow

// MARK: - EmptyLibraryState
#endif
