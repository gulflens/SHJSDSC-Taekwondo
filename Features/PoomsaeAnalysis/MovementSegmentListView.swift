#if os(iOS)
import SwiftUI

// MARK: - MovementSegmentListView
//
// Stage 2.6.a — the segment list: a collapsible side panel on iPad, a sheet
// on iPhone.
//
// One row per `MovementSegment`, showing its 0-based index, duration and peak
// motion value. Tapping a row seeks the player to the segment start; a
// long-press shows a debug popover with raw metrics (Task 6).

// MARK: - MovementSegmentListView

// MARK: - SegmentRow
#endif
