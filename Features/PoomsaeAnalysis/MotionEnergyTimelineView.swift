#if os(iOS)
import SwiftUI

// MARK: - MotionEnergyTimelineView
//
// Stage 2.6.a — the horizontal timeline beneath the video player.
//
// Draws the smoothed motion-energy curve, vertical dividers at each
// `MovementSegment` boundary, a small peak-motion dot above each segment, and
// a movable playhead bound to the player's current time.
//
// Tap-to-seek wiring (dividers + peak dots) arrives in Task 6.

// MARK: - MotionEnergyTimelineView

// MARK: - Energy curve

// MARK: - Segment dividers + peak markers

// MARK: - Playhead
#endif
